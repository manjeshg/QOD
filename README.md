This Android application will download Quote categories that are available as a JSON feed,
then randomly picks a category and fetches the Quote of the day and prompts the user to select
a means (Email/text/messenger) for sending the Quote of the day along with the title and the author.

Overview of the design
- Uses fragments and async task for fetching data from the json feed
